[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "market-llm-mvp"
version = "0.1.0"
description = "MVP for market-based LLM reasoning aggregation on GSM8K"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
